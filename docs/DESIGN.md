# 株式市場日次レポート生成ツール 設計書

## 1. プロジェクト概要

株式市場のデータ（JSON）を入力として受け取り、新聞2〜3ページ相当の見やすいHTMLレポートを生成するNode.jsツール。
公開データソースから自動取得するモードと、JSONファイルを入力するモードの両方に対応。
ブラウザで直接開ける静的HTMLファイルを出力する。

## 2. 技術スタック

| 項目 | 選定 | 理由 |
|------|------|------|
| ランタイム | Node.js | サーバー不要、CLIツールとして動作 |
| テスト | Jest | 標準的、設定が簡単 |
| テンプレート | 自前（テンプレートリテラル） | 依存ゼロで保守しやすい |
| グラフ | なし（v1） | まずはテーブル表示のみ。v2で追加予定 |
| データ取得 | Node.js 組み込み fetch | 外部依存なし（Node 18+） |

## 3. ディレクトリ構成

```
stock-report/
├── package.json
├── src/
│   ├── formatData.js      # データ整形・計算ロジック
│   ├── generateHtml.js    # HTML文字列の生成（新聞風レイアウト）
│   ├── fetchData.js       # 公開データソースからのデータ取得
│   └── main.js            # エントリーポイント（ファイル読み書き）
├── tests/
│   ├── formatData.test.js
│   ├── generateHtml.test.js
│   └── fetchData.test.js
├── data/
│   └── sample.json        # サンプル入力データ（フル版）
└── output/
    └── (生成されたHTMLがここに出力される)
```

## 4. データ構造

### 入力データ (JSON)

```json
{
  "date": "2026-03-05",
  "generatedAt": "2026-03-05T06:30:00+09:00",
  "summary": "米国市場はFRBの利下げ期待を背景にS&P500が小幅上昇。...",
  "markets": [
    {
      "name": "日経平均",
      "close": 38245.78,
      "change": 312.45,
      "changePercent": 0.82,
      "volume": 1234567890
    }
  ],
  "forex": [
    {
      "pair": "USD/JPY",
      "rate": 149.85,
      "change": -0.32,
      "changePercent": -0.21
    }
  ],
  "commodities": [
    {
      "name": "金 (Gold)",
      "price": 2145.30,
      "unit": "USD/oz",
      "change": 12.50,
      "changePercent": 0.59
    }
  ],
  "sectors": [
    {
      "name": "テクノロジー",
      "changePercent": 1.25
    }
  ],
  "news": [
    {
      "title": "FRB議長、年内利下げの可能性を示唆",
      "source": "Reuters",
      "url": "https://example.com/...",
      "category": "金融政策",
      "summary": "パウエル議長は議会証言で..."
    }
  ],
  "calendar": [
    {
      "time": "08:50",
      "country": "JP",
      "event": "GDP速報値（10-12月期）",
      "importance": "high"
    }
  ],
  "fearGreedIndex": {
    "value": 62,
    "label": "Greed"
  }
}
```

### フィールド定義

| フィールド | 型 | 説明 |
|---|---|---|
| date | string | レポート日付（YYYY-MM-DD） |
| generatedAt | string | レポート生成日時（ISO 8601） |
| summary | string | マーケット全体の要約文（3〜5文） |
| markets | array | 主要株価指数の配列 |
| forex | array | 為替データの配列 |
| commodities | array | 商品（コモディティ）データの配列 |
| sectors | array | セクター別騰落率の配列 |
| news | array | 主要ニュースヘッドラインの配列（5〜10件） |
| calendar | array | 本日の経済イベントの配列 |
| fearGreedIndex | object | 恐怖・強欲指数（市場心理指標） |

## 5. モジュール仕様

### formatData.js

| 関数 | 入力 | 出力 | 説明 |
|------|------|------|------|
| `formatPrice(number)` | `38245.78` | `"38,245.78"` | 数値をカンマ区切りに整形 |
| `formatChange(change, percent)` | `312.45, 0.82` | `"+312.45 (+0.82%)"` | 前日比を符号付き文字列に |
| `formatDate(dateStr)` | `"2026-03-05"` | `"2026年3月5日"` | 日付を日本語表記に |
| `getDirection(change)` | `312.45` | `"up"` | 上昇/下降/横ばいを判定。0は`"unchanged"` |
| `formatVolume(number)` | `1234567890` | `"12.3億"` | 出来高を億/万単位で整形 |
| `formatPercent(number)` | `1.25` | `"+1.25%"` | パーセントを符号付きで整形 |
| `formatTime(timeStr)` | `"08:50"` | `"08:50"` | 時刻をそのまま返す（将来の拡張用） |
| `getImportanceLabel(level)` | `"high"` | `"★★★"` | 重要度を星で表現 |
| `getFearGreedLabel(value)` | `62` | `{ label: "Greed", color: "orange" }` | 恐怖・強欲指数のラベルと色 |

### generateHtml.js

| 関数 | 入力 | 出力 | 説明 |
|------|------|------|------|
| `generateHtml(data)` | レポートJSON | HTML文字列 | 完全なHTMLドキュメントを生成 |

生成するHTMLの要件:
- `<!DOCTYPE html>` から始まる完全なHTML
- レスポンシブ対応（viewport meta タグ）
- 日本語（lang="ja"）
- **新聞風の2カラムレイアウト**（デスクトップ時）
- 以下のセクションを含む:
  1. ヘッダー（日付・生成時刻）
  2. マーケットサマリー（要約文）
  3. 主要株価指数テーブル
  4. 為替レートテーブル
  5. コモディティテーブル
  6. セクター別騰落ヒートマップ風表示
  7. 主要ニュース一覧（カテゴリ別）
  8. 本日の経済カレンダー
  9. Fear & Greed Index ゲージ
  10. フッター
- 上昇は緑、下降は赤で色分け
- CSSはHTML内にインライン（外部ファイル不要）
- 印刷対応（@media print）

### fetchData.js

| 関数 | 入力 | 出力 | 説明 |
|------|------|------|------|
| `fetchMarketData()` | なし | Promise<object> | 公開ソースから市場データを取得し、入力JSON形式で返す |

データソース（すべて無料・公開）:
- Yahoo Finance（公開ページ） — 株価指数・為替
- 各種公開RSS/JSONフィード — ニュースヘッドライン

注意: Web取得に失敗した場合はサンプルデータにフォールバックする

### main.js

| 関数 | 説明 |
|------|------|
| `main(inputPath, outputPath)` | JSONを読み込み→HTMLを生成→ファイルに書き出し |

CLI使用法:
```
# JSONファイルから生成
node src/main.js data/sample.json output/report.html

# データを自動取得して生成
node src/main.js --fetch output/report.html
```

## 6. エラーハンドリング

| ケース | 処理 |
|------|------|
| 入力ファイルが存在しない | エラーメッセージを表示して終了 |
| JSONのパースに失敗 | エラーメッセージを表示して終了 |
| markets配列が空 | 「データがありません」と表示するHTMLを生成 |
| データ取得に失敗 | エラーを表示し、サンプルデータで生成を続行 |
| 部分的なデータ（一部フィールド欠損） | 欠損セクションは「データなし」と表示 |

## 7. HTMLレイアウト設計

```
┌──────────────────────────────────────┐
│       株式市場デイリーレポート          │
│       2026年3月5日 (木)               │
├──────────────────────────────────────┤
│  📊 マーケットサマリー                 │
│  米国市場はFRBの利下げ期待を背景に...   │
├──────────────────┬───────────────────┤
│ 主要株価指数       │ 為替レート         │
│ ┌──┬───┬───┐   │ ┌────┬───┬───┐  │
│ │市場│終値│前日比│   │ │通貨 │レート│前日比│  │
│ ├──┼───┼───┤   │ ├────┼───┼───┤  │
│ │...│... │...  │   │ │... │... │...  │  │
│ └──┴───┴───┘   │ └────┴───┴───┘  │
├──────────────────┼───────────────────┤
│ コモディティ       │ セクター騰落       │
│ ┌──┬───┬──┐    │ ■テクノロジー +1.2% │
│ │... │... │...│    │ ■ヘルスケア  -0.3% │
│ └──┴───┴──┘    │ ■金融      +0.8% │
├──────────────────┴───────────────────┤
│  📰 主要ニュース                       │
│  ┌─────────────────────────────────┐ │
│  │ [金融政策] FRB議長、年内利下げ...  │ │
│  │ [企業決算] Apple、過去最高益を更新..│ │
│  │ [地政学] 中東情勢の緊迫化で...    │ │
│  └─────────────────────────────────┘ │
├──────────────────────────────────────┤
│  📅 本日の経済カレンダー               │
│  08:50 🇯🇵 GDP速報値 ★★★            │
│  21:30 🇺🇸 雇用統計   ★★★           │
├──────────────────────────────────────┤
│  Fear & Greed Index: 62 [Greed]      │
│  [========████████░░░░░] ← ゲージ風   │
├──────────────────────────────────────┤
│  © 2026 Stock Market Daily Report    │
└──────────────────────────────────────┘
```

## 8. 公開方法

生成したレポートを GitHub Pages で閲覧する手順:

1. `node src/main.js data/sample.json ../docs/stock-report-generated.html` で docs/ に出力
2. `docs/index.html` からリンクされているのでブラウザで閲覧可能
3. GitHub Pages の設定で `docs/` を公開ディレクトリに指定

## 9. 今後の拡張予定（v2）

- [ ] Chart.jsによるグラフ表示
- [ ] 複数日分のデータを受け取り推移表示
- [ ] ダークモード対応
- [ ] Claude API連携によるAI要約自動生成
- [ ] 定期実行（cron / GitHub Actions）
